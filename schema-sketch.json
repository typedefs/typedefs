{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Bit",
  "type": "object",

  //example of a product//
  "properties": {
    "fst": { "$ref": "#/definitions/bit" },
    "snd": { "$ref": "#/definitions/bit" }
  },
  "required": ["fst", "snd"],
  "additionalProperties": false,
  ////////////////////////

  "definitions": {
    //unit//
    "unit": {
      "enum": [
        "unit"
      ]
    },
    ////////

    //void//
    "void": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/unit"
      },
      "minItems": 2,
      "uniqueItems": true
    },
    ////////

    //example of a sum//
    "bit": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "tag": {
              "enum": [
                "left"
              ]
            },
            "value": {
              "$ref": "#/definitions/unit"
            }
          },
          "required": [
            "tag",
            "value"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "tag": {
              "enum": [
                "right"
              ]
            },
            "value": {
              "$ref": "#/definitions/unit"
            }
          },
          "required": [
            "tag",
            "value"
          ],
          "additionalProperties": false
        }
      ]
    }
    ////////////////////
  }
}